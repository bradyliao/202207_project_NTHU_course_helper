import pandas as pd
import re

from module_utility import tablepress_link_code, to_reference



# def GPA_merge(data_folder_path, global_semester, grap_GPA_semester_code, GPA_record_name, selection_system_on, GPArecord_url): 
    
    # GPA_old_df = pd.read_csv(data_folder_path + GPA_record_name)
    # GPA_new_df = pd.read_csv(data_folder_path + 'GPA_processed_' + grap_GPA_semester_code + '.csv')
    
    

        
    # course_merged = pd.DataFrame(data=newData)
    
    # course_merged.to_csv(data_folder_path + global_semester + '_course_merged.csv', index=False)